const MainContainer = ({ children }) => {
  return (
    <div className="mt-[2rem] sm:mt-[3.375rem] mb-[4rem] sm:mb-[6.75rem] px-[0.5rem] sm:px-[1.125rem]">
      {children}
    </div>
  );
};

export default MainContainer;
